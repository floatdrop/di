import { C } from '../components/prose.tsx';
import { PKG_DOC, REPO } from '../config.ts';
import { Link } from '../uikit.ts';
import type { Content } from './types.ts';

export const ja: Content = {
	locale: 'ja',
	htmlLang: 'ja',
	nativeName: '日本語',

	meta: {
		title: 'di — Go のための依存性注入',
		description:
			'ジェネリックメソッドで作られた Go 1.27+ 向けの依存性注入コンテナ。1 つのアプリケーションを上から下まで、どう構成され、どう見えるか。'
	},

	hero: {
		title: 'ジェネリックメソッドで作る Go の依存性注入。',
		lead: (
			<>
				サービスを登録し、<C>s.Get[T]()</C>
				で解決すれば、あとはコンテナが生成し、起動し、停止し、グラフを検査します。コード生成なし、依存パッケージなし。
			</>
		),
		install: 'go get github.com/floatdrop/di'
	},

	labels: {
		steps: 'ステップ',
		language: '言語',
		copy: 'インストールコマンドをコピー',
		toLight: 'ライトテーマに切り替える',
		toDark: 'ダークテーマに切り替える'
	},

	intro: (
		<>
			このページ全体が 1 つのアプリケーションで、上から順に読めます。データベースを持つ小さな HTTP
			サービス、その手前のキャッシュ、バックグラウンドで動くメーラー、そしてリクエストごとに作られるハンドラです。どのコードブロックもリポジトリの
			<Link href={`${REPO}/tree/main/examples/guide`}>
				<C>examples/guide</C>
			</Link>
			にあるファイルそのものです。Go
			のツールチェーンが変更のたびにコンパイルしてテストしているので、見えているものがそのまま動くものです。
		</>
	),

	tree: [
		{ path: 'cmd/api/main.go', comment: 'モジュールを組み、グラフを検査し、実行する' },
		{ path: 'internal/config/', comment: '設定。値として登録される' },
		{ path: 'internal/storage/', comment: 'データベースと、その上に作られるストア' },
		{ path: 'internal/cache/', comment: 'ストアを包むキャッシュ' },
		{ path: 'internal/mail/', comment: 'バックグラウンドの worker' },
		{ path: 'internal/api/', comment: 'HTTP サーバとそのハンドラ' }
	],

	steps: [
		{
			id: 'shape',
			title: 'アプリケーションの形',
			body: (f) => (
				<>
					<p>
						各パッケージは自分のサービスを持ち、それらを登録する<C>Module</C>という関数を 1
						つだけ公開します。それ以外にパッケージに特別なところはありません。コンストラクタはただの関数、型はただの型で、グラフ全体を知っているファイルは
						<C>main</C>だけです。
					</p>
					{f.tree}
				</>
			)
		},
		{
			id: 'constructors',
			title: 'コンストラクタとモジュール',
			body: (f) => (
				<>
					<p>
						<C>newDB</C>と<C>newPGStore</C>は必要なものを引数で受け取り、作ったものを返します。
						<C>newDB</C>
						は失敗しえます。どちらもコンテナを import
						しません。このアプリケーションでは、スコープを受け取るクロージャを渡す汎用の形<C>Provide</C>
						はどこにも必要ありませんが、必要になれば両者は自由に混ぜられます。
					</p>
					<p>
						<C>Module</C>は<C>Wire</C>
						でそれらを渡します。型引数は、そのサービスが提供されるときのキーです。接続には<C>*db</C>、
						ストアにはインターフェースの<C>Store</C>。<C>*pgStore</C>
						がそれに代入可能だからです。<C>Wire</C>
						に渡したコンストラクタの引数がそのまま依存であり、コンテナが何も作らないうちにグラフを知っているのはそのためです。フックは受け取る値に対して型付けされ、アプリケーションの起動時と停止時に走ります。
					</p>
					<p>
						プライバシーは Go のものそのままです。キーは型なので、このパッケージだけが名前を書ける
						<C>*db</C>
						は、このパッケージだけが解決できるサービスになります。パッケージが公開するのは契約である
						<C>Store</C>と<C>User</C>、そして<C>Module</C>
						だけ。接続にはコンテナが動かすライフサイクルがありますが、それ以外は誰にも関係ありません。
					</p>
					{f.storage}
				</>
			)
		},
		{
			id: 'config',
			title: '設定は値である',
			body: (f) => (
				<>
					<p>
						すでに手元にある値は<C>Value</C>で登録します。ほかのキーと何も変わりません。<C>NewDB</C>
						は引数として受け取り、テストは<C>Override()</C>で差し替え、下流のサービスはすべてそれに従います。
					</p>
					{f.config}
				</>
			)
		},
		{
			id: 'main',
			title: 'main での組み立て',
			body: (f) => (
				<>
					<p>
						<C>Use</C>はモジュールを順に適用し、それぞれの登録をそれを行ったモジュールに紐づけます。2
						つのモジュールが衝突したときにエラーが名指しするのがこれです。同じキーの 2 回目の登録は
						<C>Override()</C>
						がなければ両方を名指しして拒否されるので、あるモジュールが別のモジュールの配線を黙って書き換えることはできません。
					</p>
					<p>
						<C>Validate</C>
						は何も生成せずに宣言されたグラフをたどります。そのとき、リクエストスコープが何を持つことになるかを伝えておきます。誰も提供しない依存、循環、シングルトンに捕まったリクエストスコープのサービスは、最初のリクエストではなく起動時のここで失敗します。そのあと
						<C>Run</C>が eager
						なサービスを起動し、シグナルを待ち、タイムアウトの範囲内で逆順にすべてを停止します。
					</p>
					{f.main}
				</>
			)
		},
		{
			id: 'workers',
			title: 'バックグラウンドの worker',
			body: (f) => (
				<>
					<p>
						<C>Go</C>で登録した worker
						はそのサービスと同じだけ生きます。サービスの起動時に専用のゴルーチンで開始され、<C>Stop</C>
						でキャンセルされ、それが依存している何かが片付けられる前に完了を待たれます。ここからエラーを返すとアプリケーションが停止します。
						<C>Eager</C>は、メーラーが最初の利用時ではなく<C>Start</C>
						が返る時点ですでに存在していることを意味します。
					</p>
					{f.mail}
				</>
			)
		},
		{
			id: 'http',
			title: 'HTTP とリクエストスコープ',
			body: (f) => (
				<>
					<p>
						<C>dihttp.Middleware</C>はリクエストごとに子スコープを開き、そこに<C>*http.Request</C>
						を持たせます。アプリケーションスコープで<C>Scoped</C>
						と宣言されたサービスは、シングルトンからでもリクエスト固有の値からでも同じように、リクエストスコープごとに
						1 回だけ生成され、スコープとともに停止します。ハンドラは<C>di.FromContext</C>
						で自分のスコープに辿り着くか、ハンドラ型のメソッドについてそれを代わりに行う
						<C>dihttp.Handle</C>を使います。
					</p>
					<p>
						1 つのハンドラ型が 1
						つのリソースを受け持ち、ルートごとにメソッドを持つので、依存の宣言は一度で済みます。
						<C>dihttp.Handle((*Users).Show)</C>
						はリクエストスコープからその型を解決してメソッドを呼びます。メソッド式が両方を名指しするので型引数は要りません。
						<C>Users</C>は呼び出し元を必要とするので<C>Scoped</C>、<C>Health</C>
						はリクエストから何も取らない普通のシングルトンで、<C>Handle</C>
						はどちらのライフタイムにも従います。
					</p>
					<p>
						middleware 自身がリクエストごとに子を開くためのスコープを必要とするので、
						<C>dihttp.Module</C>
						がそれをサービスとして登録し、サーバはほかと同じように引数で受け取ります。<C>OnDrain</C>
						は何かが停止される前に走るので、<C>http.Server.Shutdown</C>
						が待っている間、処理中のリクエストは自分のスコープを保ったままです。
					</p>
					{f.api}
				</>
			)
		},
		{
			id: 'wrap',
			title: '置き換えずに包む',
			body: (f) => (
				<>
					<p>
						<C>Wrap</C>
						は、そのキーをすでに提供しているものの上に重ねます。ラッパーはその値を最初の引数で受け取り、ほかの依存はその後ろに並べます。ストアは自分の登録とフックをそのまま保ち、先に生成され、ラッパーより後に停止し、ラッパーは自分が変えないものをそのまま素通しします。唯一気をつけるのはモジュールの順序で、キャッシュのモジュールは
						storage
						の後に来ます。子スコープで登録されたラッパーは、そのスコープとその子孫にだけ効きます。
						storage と同じくこのパッケージも<C>Module</C>
						だけを公開します。横断的関心事は公開された契約の上に重ねるもので、パッケージの内部の上ではありません。
					</p>
					{f.cache}
				</>
			)
		},
		{
			id: 'testing',
			title: 'オーバーライドによるテスト',
			body: (f) => (
				<>
					<p>
						<C>di.Test</C>
						はモジュールを新しいスコープに配線し、テストが終わるとそれを停止します。設定をオーバーライドするだけでストアを別のデータベースに向けられますし、偽物にするなら
						<C>s.Value(&amp;fake).Override()</C>でまったく同じです。このマーカーは必須で、これがない 2
						回目の登録は拒否されるため、テストが本番の配線のまま偶然通ってしまうことはありません。
					</p>
					{f.storageTest}
					<p>
						ラッパーは同じモジュールを通して、何も偽物にせずにテストします。2 回引いて 1
						回ヒット。自分のキャッシュに名前を書けるのは cache パッケージだけなので、これは内部テストです。
					</p>
					{f.cacheTest}
				</>
			)
		},
		{
			id: 'graph',
			title: 'グラフを見る',
			body: (f) => (
				<>
					<p>
						何も作られないうちから、<C>Explain</C>は<C>Wire</C>
						に渡されたコンストラクタが宣言した内容を描きます。破線の辺、ストアの上のラッパー、そして尋ねたサービスを宣言しているのは誰か。生成後は実際に起きたことを実線で描き、続けてそれを必要としたものを描きます。
						<C>Graph</C>はアプリケーション全体を Graphviz DOT として出力します。これは起動時の
						<C>app.Explain[storage.Store]()</C>で、リポジトリのテストで固定されています。
					</p>
					{f.explain}
					<p>
						<C>Modules</C>
						は同じ情報をサービス単位ではなくモジュール単位で読んだものです。各モジュールが何を提供し、何を必要としてそれを誰が提供するか、何を包んでいるか、そしてどのコンストラクタがクロージャか。登録内容から導かれるので、別途維持すべきマニフェストはありません。これは何も作られる前のアプリケーション全体で、これもテストで固定されています。
					</p>
					{f.modules}
				</>
			)
		},
		{
			id: 'run',
			title: '動かす',
			body: (f) => (
				<>
					<p>
						Ctrl-C
						はサーバに処理中のリクエストを終わらせ、メーラーをキャンセルし、データベースを閉じます。まさにこの順序で、そして失敗したフックがあればすべて報告します。
					</p>
					{f.run}
					<p>
						<Link href={`${REPO}#readme`}>README</Link>
						が残りを扱います。グループ、オブザーバ、そしてコンテナが強制するルール。
						<Link href={`${REPO}/blob/main/docs/DESIGN.md`}>仕組み</Link>は逆向きに、<C>Get</C>
						から値までをたどります。ライフタイム、フェーズ、循環、シャットダウンを図つきで。
					</p>
				</>
			)
		}
	],

	footer: (
		<>
			<Link href={REPO}>github.com/floatdrop/di</Link> · <Link href={PKG_DOC}>pkg.go.dev</Link> ·
			MIT ライセンス · このページはリポジトリの<C>site/</C>
			ディレクトリから作られ、掲載しているコードは<C>examples/guide/</C>のものです。
		</>
	)
};
