import { Link } from '../uikit.ts';

import { C } from '../components/prose.tsx';
import { PKG_DOC, REPO } from '../config.ts';
import type { Content } from './types.ts';

export const ru: Content = {
	locale: 'ru',
	htmlLang: 'ru',
	nativeName: 'Русский',

	meta: {
		title: 'di — dependency injection для Go',
		description:
			'DI-контейнер для Go 1.27+ на дженерик-методах. Одно приложение сверху донизу: как оно устроено и как выглядит.'
	},

	hero: {
		title: 'Dependency injection для Go на дженерик-методах.',
		lead: (
			<>
				Регистрируйте сервисы, резолвите их через <C>s.Get[T]()</C> — контейнер сам создаст,
				запустит, остановит и проверит граф. Без кодогенерации и без зависимостей.
			</>
		),
		install: 'go get github.com/floatdrop/di'
	},

	labels: {
		steps: 'Шаги',
		language: 'Язык',
		copy: 'Скопировать команду установки',
		toLight: 'Переключить на светлую тему',
		toDark: 'Переключить на тёмную тему'
	},

	intro: (
		<>
			Эта страница — одно приложение, которое читается сверху вниз: небольшой HTTP-сервис с базой
			данных, кэшем перед ней, мейлером в фоне и обработчиком, который создаётся на
			каждый запрос. Каждый блок кода — это файл из{' '}
			<Link href={`${REPO}/tree/main/examples/guide`}>
				<C>examples/guide</C>
			</Link>{' '}
			в репозитории. Go-тулчейн компилирует и тестирует их при каждом изменении, так что вы
			видите ровно то, что работает.
		</>
	),

	tree: [
		{ path: 'cmd/api/main.go', comment: 'собирает модули, проверяет граф, запускает' },
		{ path: 'internal/config/', comment: 'настройки, зарегистрированные как значение' },
		{ path: 'internal/storage/', comment: 'база данных и хранилище поверх неё' },
		{ path: 'internal/cache/', comment: 'кэш, обёрнутый вокруг хранилища' },
		{ path: 'internal/mail/', comment: 'фоновый воркер' },
		{ path: 'internal/api/', comment: 'HTTP-сервер и его обработчики' }
	],

	steps: [
		{
			id: 'shape',
			title: 'Устройство приложения',
			body: (f) => (
				<>
					<p>
						Каждый пакет владеет своими сервисами и предоставляет одну функцию, <C>Module</C>,
						которая их регистрирует. Больше в пакете ничего особенного: конструкторы — обычные
						функции, типы — обычные типы, и единственный файл, знающий весь граф, — это{' '}
						<C>main</C>.
					</p>
					{f.tree}
				</>
			)
		},
		{
			id: 'constructors',
			title: 'Конструкторы и модуль',
			body: (f) => (
				<>
					<p>
						<C>newDB</C> и <C>newPGStore</C> принимают параметрами то, что им нужно, и
						возвращают то, что создают; <C>newDB</C> может завершиться ошибкой. Ни тот, ни
						другой не импортирует контейнер. Этому приложению нигде не нужна общая форма,{' '}
						<C>Provide</C>, принимающая замыкание над скоупом; но обе формы свободно
						сочетаются, когда она всё-таки понадобится.
					</p>
					<p>
						<C>Module</C> передаёт их через <C>Wire</C>. Аргумент типа — это ключ, под которым
						отдаётся сервис: <C>*db</C> для соединения и интерфейс <C>Store</C> для хранилища,
						поскольку <C>*pgStore</C> присваиваем этому интерфейсу. Параметры конструктора, переданного в <C>Wire</C>, —
						это его зависимости, и именно так контейнер знает граф ещё до того, как хоть что-то
						создано. Хуки типизированы значением, которое получают, и выполняются при старте и
						остановке приложения.
					</p>
					<p>
						Приватность здесь — приватность самого Go. Ключи — это типы, поэтому <C>*db</C>,
						который может назвать только этот пакет, — сервис, который только этот пакет может
						зарезолвить. Пакет экспортирует свой контракт, <C>Store</C> и <C>User</C>, и свой{' '}
						<C>Module</C>; у соединения есть жизненный цикл, которым управляет контейнер, а в
						остальном оно никого не касается.
					</p>
					{f.storage}
				</>
			)
		},
		{
			id: 'config',
			title: 'Конфигурация — это значение',
			body: (f) => (
				<>
					<p>
						Уже готовое значение регистрируется через <C>Value</C>. Это такой же ключ, как
						любой другой: <C>NewDB</C> получает его параметром, а тест подменяет его через{' '}
						<C>Override()</C> — и все сервисы ниже по графу подхватывают подмену.
					</p>
					{f.config}
				</>
			)
		},
		{
			id: 'main',
			title: 'Композиция в main',
			body: (f) => (
				<>
					<p>
						<C>Use</C> применяет модули по порядку и приписывает каждую регистрацию тому
						модулю, который её сделал, — именно это называет ошибка, когда два модуля
						сталкиваются. Повторная регистрация ключа без <C>Override()</C> отклоняется с
						указанием обеих, так что один модуль не сможет незаметно перенастроить другой.
					</p>
					<p>
						<C>Validate</C> обходит объявленный граф, ничего не создавая, — ему сообщают, что
						будет лежать в скоупе запроса. Зависимость, которую никто не предоставляет, цикл
						или сервис со временем жизни запроса, захваченный синглтоном, — всё это падает
						здесь, на старте, а не на первом запросе. Затем <C>Run</C> запускает eager-сервисы,
						ждёт сигнала и останавливает всё в обратном порядке, укладываясь в таймаут.
					</p>
					{f.main}
				</>
			)
		},
		{
			id: 'workers',
			title: 'Фоновые воркеры',
			body: (f) => (
				<>
					<p>
						Воркер, зарегистрированный через <C>Go</C>, живёт ровно столько, сколько его сервис: запускается в собственной
						горутине, когда сервис стартует, отменяется по <C>Stop</C>, и его дожидаются
						прежде, чем начать останавливать то, от чего он зависит. Возврат ошибки из него
						останавливает приложение. <C>Eager</C> говорит, что мейлер существует уже к
						моменту возврата из <C>Start</C>, а не создаётся при первом обращении.
					</p>
					{f.mail}
				</>
			)
		},
		{
			id: 'http',
			title: 'HTTP и скоупы запроса',
			body: (f) => (
				<>
					<p>
						<C>dihttp.Middleware</C> открывает дочерний скоуп на каждый запрос и держит в нём{' '}
						<C>*http.Request</C>. Сервисы, объявленные <C>Scoped</C> в скоупе приложения,
						создаются по одному разу на скоуп запроса — из синглтонов и из значений самого
						запроса одинаково — и останавливаются вместе с ним. Обработчик добирается до своего
						скоупа через <C>di.FromContext</C> или через <C>dihttp.Handle</C>, который делает
						это за метод типа-обработчика.
					</p>
					<p>
						Тип-обработчик покрывает один ресурс, по методу на маршрут, поэтому его зависимости
						объявляются один раз. <C>dihttp.Handle((*Users).Show)</C> резолвит тип из скоупа
						запроса и вызывает метод; выражение метода называет и то и другое, так что аргумент
						типа не нужен. <C>Users</C> объявлен <C>Scoped</C>, потому что ему нужен
						вызывающий; <C>Health</C> ничего не берёт из запроса и является обычным синглтоном,
						а <C>Handle</C> работает с любым временем жизни.
					</p>
					<p>
						Самому middleware нужен скоуп, чтобы открывать дочерний на каждый запрос, поэтому{' '}
						<C>dihttp.Module</C> регистрирует его как сервис, а сервер принимает его
						параметром, как и всё остальное. <C>OnDrain</C> выполняется до того, как что-либо
						будет остановлено, поэтому запросы в полёте сохраняют свои скоупы, пока{' '}
						<C>http.Server.Shutdown</C> их дожидается.
					</p>
					{f.api}
				</>
			)
		},
		{
			id: 'wrap',
			title: 'Обёртка вместо замены',
			body: (f) => (
				<>
					<p>
						<C>Wrap</C> надстраивается над тем, что уже отдаёт ключ. Обёртка принимает это
						значение первым параметром, а остальные свои зависимости — после него. Хранилище
						сохраняет свою регистрацию и свои хуки, создаётся первым и останавливается после
						обёртки, а обёртка пробрасывает дальше всё, что не меняет. Единственное, что важно
						не перепутать, — порядок модулей: модуль кэша идёт после модуля storage. Обёртка,
						зарегистрированная в дочернем скоупе, действует только в нём и его потомках. Как и
						storage, этот пакет экспортирует только свой <C>Module</C>: сквозная
						функциональность надстраивается над экспортированным контрактом, а не над
						внутренностями пакета.
					</p>
					{f.cache}
				</>
			)
		},
		{
			id: 'testing',
			title: 'Тестирование через переопределение',
			body: (f) => (
				<>
					<p>
						<C>di.Test</C> связывает модули в свежем скоупе и останавливает его, когда тест
						заканчивается. Достаточно переопределить конфигурацию, чтобы направить хранилище в
						другую базу; заглушка выглядела бы точно так же:{' '}
						<C>s.Value(&amp;fake).Override()</C>. Маркер обязателен: повторная регистрация без
						него отклоняется, поэтому тест не сможет случайно пройти на боевой конфигурации.
					</p>
					{f.storageTest}
					<p>
						Обёртка тестируется через те же модули, без единой заглушки: два обращения, одно
						попадание. Это внутренний тест, потому что назвать собственный кэш может только сам
						пакет cache.
					</p>
					{f.cacheTest}
				</>
			)
		},
		{
			id: 'graph',
			title: 'Как увидеть граф',
			body: (f) => (
				<>
					<p>
						Ещё до того, как что-то создано, <C>Explain</C> рисует то, что объявили конструкторы,
						переданные в <C>Wire</C>: пунктирные рёбра, обёртку над хранилищем и то, какой сервис объявляет
						тот, о котором вы спросили. После создания он рисует сплошными линиями то, что
						произошло на самом деле, а следом — кому это понадобилось. <C>Graph</C> отдаёт всё
						приложение в виде Graphviz DOT. Это <C>app.Explain[storage.Store]()</C> на старте,
						зафиксированный тестом в репозитории.
					</p>
					{f.explain}
					<p>
						<C>Modules</C> — та же информация, прочитанная по модулям, а не по сервисам: что
						каждый предоставляет, что ему нужно и кто это отдаёт, что он оборачивает и какие из
						его конструкторов — замыкания. Она выводится из самих регистраций, так что нет
						манифеста, который пришлось бы поддерживать в актуальном виде. Это всё приложение
						до того, как что-либо создано, и тоже зафиксировано тестом.
					</p>
					{f.modules}
				</>
			)
		},
		{
			id: 'run',
			title: 'Запуск',
			body: (f) => (
				<>
					<p>
						Ctrl-C даёт серверу завершить запросы в полёте, отменяет мейлер, закрывает базу
						— именно в этом порядке — и сообщает о каждом хуке, который завершился ошибкой.
					</p>
					{f.run}
					<p>
						<Link href={`${REPO}#readme`}>README</Link> покрывает остальное: группы,
						наблюдатели и правила, которые контейнер обеспечивает.{' '}
						<Link href={`${REPO}/blob/main/docs/DESIGN.md`}>Как это устроено</Link> идёт в
						обратную сторону, от <C>Get</C> к значению: времена жизни, фазы, циклы и завершение
						работы, с диаграммами.
					</p>
				</>
			)
		}
	],

	footer: (
		<>
			<Link href={REPO}>github.com/floatdrop/di</Link> ·{' '}
			<Link href={PKG_DOC}>pkg.go.dev</Link> · Лицензия MIT · Эта страница собрана из каталога{' '}
			<C>site/</C> в репозитории, а показанный на ней код — из <C>examples/guide/</C>.
		</>
	)
};
