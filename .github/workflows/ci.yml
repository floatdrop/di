name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          check-latest: true
          cache-dependency-path: benchmarks/go.sum
      - name: gofmt
        run: test -z "$(gofmt -l .)" || (gofmt -d . && exit 1)
      - name: vet
        run: go vet ./...
      - name: lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.1
      - name: README examples are in sync
        run: go run github.com/campoy/embedmd@v1.0.0 -d README.md
      - name: run examples
        run: |
          go run ./examples/quickstart
          go run ./examples/scopes
      - name: test
        run: go test -race -count=1 -coverprofile=coverage.out ./...
      - name: what only the hand-written tests reach
        run: |
          go test -count=1 -run 'TestMachine|TestConcurrent|TestProperty|FuzzMachine' -coverprofile=generators.out .
          go test -count=1 -coverprofile=everything.out .
          go run scripts/generatorgap.go -floor 85 generators.out everything.out
          # The gap report is only worth its floor if it reads a profile the
          # way the toolchain does. It once keyed coverage blocks by line and
          # merged the eighteen lines of di.go that carry more than one, which
          # made a hook body look covered because its registration was.
          want=$(go tool cover -func=generators.out | tail -1 | awk '{print $NF}')
          got=$(go run scripts/generatorgap.go generators.out everything.out | grep -oE '[0-9]+\.[0-9]+%' | head -1)
          if [ "$want" != "$got" ]; then
            echo "::error::generatorgap reports $got where go tool cover reports $want"
            exit 1
          fi
      - name: benchmarks compile and run
        working-directory: benchmarks
        run: |
          go vet ./...
          go test -run '^$' -bench . -benchtime=50ms ./...

  fuzz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          check-latest: true
          cache-dependency-path: benchmarks/go.sum
      - name: fuzz the container against its invariants
        run: go test -run '^$' -fuzz 'FuzzMachine$' -fuzztime 90s .
      - name: fuzz the concurrent oracles
        run: go test -race -run '^$' -fuzz FuzzMachineConcurrent -fuzztime 90s .
      - name: upload any failing input
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-corpus
          path: testdata/fuzz/
